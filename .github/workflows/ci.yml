name: CI

on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plugin-build:
    name: Plugin Build and Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Restore plugin dependencies
        run: dotnet restore src/Jellyfin.Plugin.Shirarium/Jellyfin.Plugin.Shirarium.csproj

      - name: Restore plugin test dependencies
        run: dotnet restore tests/Jellyfin.Plugin.Shirarium.Tests/Jellyfin.Plugin.Shirarium.Tests.csproj

      - name: Restore plugin integration test dependencies
        run: dotnet restore tests/Jellyfin.Plugin.Shirarium.IntegrationTests/Jellyfin.Plugin.Shirarium.IntegrationTests.csproj

      - name: Build plugin
        run: dotnet build src/Jellyfin.Plugin.Shirarium/Jellyfin.Plugin.Shirarium.csproj -c Release --no-restore

      - name: Run plugin unit tests
        run: dotnet test tests/Jellyfin.Plugin.Shirarium.Tests/Jellyfin.Plugin.Shirarium.Tests.csproj -c Release --no-restore --verbosity normal

      - name: Run plugin integration tests
        run: dotnet test tests/Jellyfin.Plugin.Shirarium.IntegrationTests/Jellyfin.Plugin.Shirarium.IntegrationTests.csproj -c Release --no-restore --verbosity normal

      - name: Upload plugin build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: jellyfin-plugin-shirarium-release
          path: src/Jellyfin.Plugin.Shirarium/bin/Release/
